Drop your image assets here to replace the in-app placeholders.

Expected files (exact names):
  agent-portrait.png   ~512x512   large companion portrait (chat panel)
  agent-avatar.png     ~256x256   round avatar for the floating button
  hero.png             ~256x256   the adventurer's portrait

Until a file exists, the app shows a labeled placeholder so nothing breaks.
No code change is needed - just add the file and rebuild.

Slots are defined in src/assets/placeholders.ts if you want to add more.
