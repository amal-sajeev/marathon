{
  "name": "rpgtask-push",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker that sends scheduled Web Push check-in reminders for RPGtask.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "gen-vapid": "node scripts/gen-vapid.mjs"
  },
  "dependencies": {
    "webpush-webcrypto": "^1.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
